# Scope Playground Example

Showcases:
- local dependency scope with `LScope.put`
- isolated controller instances per subtree
- deterministic lifecycle cleanup per panel

Run:

```bash
flutter run
```
