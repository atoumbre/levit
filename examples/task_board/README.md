# Task Board Example

Showcases a practical Flutter flow with:
- scoped controller lifecycle
- list mutation + derived selectors
- explicit (`LSelectorBuilder`) and implicit (`LWatch`) rebuild paths

Run:

```bash
flutter run
```
