name: levit_mcp_server
description: MCP server exposing Levit-focused tooling over stdio.
version: 0.1.0
publish_to: none
repository: https://github.com/softilab/levit

environment:
  sdk: ^3.0.0

dependencies:
  levit_reactive:
    path: ../../packages/core/levit_reactive

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
