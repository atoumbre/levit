Levit LLM Guidance (Short)
==========================

Ownership-first runtime: scope → controller → reactive → Flutter bindings.

When to read:
- This file — routine edits and quick orientation (default).
- LLM.txt — scopes, stores, async DI, middleware, or unfamiliar APIs.
- WHY_LEVIT.md — architecture rationale and tradeoffs.

Before coding: identify the lifetime boundary (app / route / feature / subtree /
test), then pick the matching scope widget or Levit.createScope.

Imports
-------

- Flutter: package:levit_flutter/levit_flutter.dart
- Pure Dart: package:levit/levit.dart
- Core packages only when kits are insufficient.

Layering: levit_flutter → levit_flutter_core + levit_dart → levit_dart_core →
levit_scope + levit_reactive. Add levit_monitor only for diagnostics.

Examples: task_board, route_scopes, scope_playground, async_catalog,
nexus_studio.


Bootstrap (Flutter)
-------------------

Call Levit.enableAutoLinking() in main() before runApp.
Still autoDispose non-reactive resources and reactives created outside
controller init.


Dependency Injection
--------------------

- Levit.put(() => ...) — eager register
- Levit.lazyPut(() => ...) — lazy register
- Levit.lazyPutAsync(() async => ...) — async lazy; resolve with findAsync
- Levit.bindExisting<Port, Implementation>() — non-owning local singleton alias
- Levit.find<T>() — resolve from active scope (pure Dart / no widget scope)
- context.levit.find<T>() — resolve from nearest widget scope (Flutter)
- tag, isFactory — disambiguation / per-resolve factories
- permanent: true — survives non-forced reset only; scope dispose() always
  force-clears (no leak across disposed widget scopes / widget tests)

Register app-wide deps in root scope; feature/route deps in owning scope.
Page-scoped LScopedView and root-permanent feature controllers are both valid.
Use LevitStore / LevitAsyncStore for portable per-scope state recipes.
Always await Levit.delete, Levit.reset, scope.dispose, and Levit.runInScope.
An instantiated registration must be deleted and awaited before replacement.

Flutter scope widgets: LScope, LAsyncScope, LScope.put, LScopedView,
LScopedAsyncView, LRouteScope, LAsyncRouteScope, LScope.capture,
showLevitDialog / showLevitModalBottomSheet (kit).
Omitted name/scopeName values are auto-unique; set them only for stable labels.
Do not manually create/dispose scopes in normal widget code.


Reactive State
--------------

- 0.lx — reactive value; read/write via count() or count.value
- .named('label') — diagnostics
- LxList / LxMap / LxSet or <T>[].lx — collections
- LxComputed(() => ...) — derived state
- LxComputed.async(() async => ...) — async derived state
- LxAsyncConcurrency.exhaustLatest — one running + one coalesced trailing run
- LxStream completionPolicy: retain — preserve/rebind a completed source
- LxFamily<K, R> — lazy keyed reactive resources with explicit eviction
- LxStatus<T> + LStatusBuilder — async UI states
- LxWorker — side effects (prefer autoDispose(LxWorker(...)) for reload)
- Levit.batch / Levit.batchAsync — coalesce multiple updates


Controllers
-----------

Extend LevitController for feature/screen logic. Keep widgets thin.
Use own / autoDispose for resources; cleanup is awaited, LIFO, and idempotent.
Use LevitResourceOwnership on non-controller LevitScopeDisposable resources.
Always call super.onInit / await super.onClose in overrides.
Reload on context changes:

  autoDispose(LxWorker(contextVar, (_) async { await reload(); }));


Flutter Widgets
---------------

- LWatch — implicit reactive rebuild (partial trees / autoWatch: false)
- LView / LScopedView autoWatch: true (default) — read reactives in buildView;
  do not wrap the entire return in LWatch
- LBuilder / LSelectorBuilder / LStatusBuilder — page UI kits
- LView / LAsyncView — consume existing scope dependency
- LScopedView.put or subclass + onConfigScope — create scope + controller

Kit extras (levit_flutter): showLevitDialog, showLevitModalBottomSheet,
LKeepAlive, LevitAppLifecycleMixin.

GoRouter: shell + nested LScopedView/LView is supported; LRouteScope is for
route-tied ownership (examples/route_scopes).


Widget tests
------------

Mount LScope + dependencyFactory; capture scope in the factory and await
scope disposal. Prefer isolated scopes over global reset.


Pure Dart
---------

- await Levit.runInScope(() => ...) — tests and short-lived work
- Levit.createScope + await dispose — long-lived ownership


Tasks (levit_dart)
------------------

- LevitReactiveTasksMixin — runTask, task selectors, blocking/progress state
- LevitTasksMixin — engine-only scheduling/retries
- TaskConflictPolicy — reject (default), join, drop, restart, enqueue,
  coalesceLatest
- LevitTaskContext — cooperative cancellation, progress, execution metadata
- LevitTaskMiddleware / LevitTaskTracker — structured events / aggregate state
- scheduleIsolate / submitIsolate / runIsolateTask — explicit isolate work
- LevitTimeMixin, LevitSelectionMixin, LevitLoopExecutionMixin


Middleware
----------

- Levit.addDependencyMiddleware / removeDependencyMiddlewareByToken — DI events
- Levit.addStateMiddleware / removeStateMiddlewareByToken — reactive mutations
- LevitTaskMiddleware.add / removeByToken — task lifecycle events
Use one stable token per concern.

levit_monitor remains independent of levit_dart. Bridge task events through
LevitMonitor.emitCustomEvent in the application composition layer.

Enable optional levit_lints in analysis_options.yaml for lifecycle, ownership,
registration, and reactive-status diagnostics (Dart 3.10+).


Anti-Patterns
-------------

- Feature state in global scope
- Levit.find in widgets (use context.levit.find)
- Large subtree rebuilds for small reactive reads
- Outer LWatch wrapping entire LView/LScopedView buildView when autoWatch is true
- Business logic in widgets
- Manual scope plumbing in ordinary UI
- Low-level core imports in app code
- Imperative sheets/dialogs without LScope.capture / showLevit*


Validation
----------

```bash
melos run format
melos run analyze --no-select
melos run test --no-select
melos run test:flutter --no-select
melos run ci --no-select
```

Update DartDoc and package README when changing public APIs.
