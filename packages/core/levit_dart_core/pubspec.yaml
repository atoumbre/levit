name: levit_dart_core
description: Core Dart runtime for Levit scopes, controllers, stores, and reactive ownership.
repository: https://github.com/softilab/levit
version: 0.0.8
topics:
  - state-management
  - dependency-injection
  - framework

environment:
  sdk: ^3.0.0

dependencies:
  levit_scope: ^0.0.8
  levit_reactive: ^0.0.8
  meta: ^1.17.0

dev_dependencies:
  test: ^1.29.0
  rxdart: ^0.28.0
  lints: ^6.0.0
