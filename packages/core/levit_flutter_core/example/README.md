# levit_flutter_core example

Balanced task-board demo for `levit_flutter_core`.

It showcases:
- scoped controller lifecycle with `LScopedView.put`
- implicit and explicit rebuilds (`LWatch`, `LBuilder`)
- derived state with `LSelectorBuilder`
- async UX states with `LStatusBuilder`

Run:

```bash
flutter run
```
