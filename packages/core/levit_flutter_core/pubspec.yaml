name: levit_flutter_core
description: Flutter bindings for Levit scopes, rebuild boundaries, and widget-tree ownership.
repository: https://github.com/softilab/levit
version: 0.0.8
topics:
  - flutter
  - state-management
  - widgets

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  levit_dart_core: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_async: ^1.3.3
  lints: ^6.0.0
