name: levit_scope
description: Hierarchical dependency scopes with deterministic ownership and teardown.
repository: https://github.com/softilab/levit
version: 0.0.9
topics:
  - dependency-injection
  - service-locator
  - di

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
  coverage: ^1.15.0
