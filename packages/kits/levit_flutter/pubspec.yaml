name: levit_flutter
description: Deterministic ownership and fine-grained reactivity for Flutter applications.
version: 0.0.8
repository: https://github.com/softilab/levit
topics:
  - flutter
  - utilities
  - widgets

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  levit_dart: ^0.0.8
  levit_flutter_core: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.1.0
