name: levit_dart
description: Task, loop, and controller utilities built on top of the Levit Dart runtime.
version: 0.0.8
repository: https://github.com/softilab/levit
topics:
  - utilities
  - mixins
  - tasks

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  levit_dart_core: ^0.0.8
  meta: ^1.11.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
